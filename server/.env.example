# Port for Express server (defaults to 10000 on Render)
PORT=10000

# Render PostgreSQL connection string (Automatically injected when linked in Render)
# Example: postgres://carmarket_user:password@dpg-xxxx.render.com/carmarket
DATABASE_URL=
