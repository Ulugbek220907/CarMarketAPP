services:
  - type: web
    name: carmarket-api
    runtime: node
    plan: free
    region: oregon
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: carmarket-db
          property: connectionString
      - key: NODE_ENV
        value: production

databases:
  - name: carmarket-db
    plan: free
    region: oregon
    databaseName: carmarket
    user: carmarket_user
